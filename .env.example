ALPHA_RPC_URL=https://alphanet.load.network
CHAIN_ID=9496

# Deployment Scripts (Foundry)
# ORACLE=0xOracleAddress
# FACTORY=0xFactoryAddress
# ADAPTER=0xAdapterAddress
# FEED_DESC="AR/byte"
# FEEDS_FILE=feeds/feeds.json          # Path to JSON feed configuration file
# FEEDS_DESC_FILE=feeds.json           # JSON array of strings, e.g. ["AR/byte","AR/byte:v2"]
# OPERATORS_FILE=operators.json        # JSON array of addresses, e.g. ["0x...","0x..."]
# DECIMALS=8
# MIN_SUBMISSIONS=2
# MAX_SUBMISSIONS=3
# HEARTBEAT_SEC=3600
# DEVIATION_BPS=50
# TIMEOUT_SEC=900
# MIN_PRICE=0
# MAX_PRICE=10000000000000000000000
# DESCRIPTION="AR/byte"

# Operator Bot (Node.js)
# RPC_URL=http://127.0.0.1:8545        # RPC endpoint for bot
# ORACLE=0xOracleAddress                # Oracle contract address
# FEED_DESC="ar/bytes-testv1"          # Feed identifier
# FEED_ID=0x3f32666a...                 # Alternative: use feedId directly
# INTERVAL_MS=30000                     # Submission interval in milliseconds
# NUM_OPS=6                             # Number of operators to use
# PRIVATE_KEYS_JSON='["0x...","0x..."]' # Optional: custom operator keys (defaults to Anvil keys)

# Integration Testing (Node.js)
# CONSUMER=0xConsumerAddress            # TestPriceConsumer contract address
# ADAPTER=0xAdapterAddress              # Adapter contract address for testing
